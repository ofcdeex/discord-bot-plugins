module.exports = {
    event: "ready",
    execute(client, func, MessageActionRow, MessageButton, PluginSettings) {
        console.log('WL LOADED');
        console.log('WL LOADED');
        console.log('WL LOADED');
    }
}